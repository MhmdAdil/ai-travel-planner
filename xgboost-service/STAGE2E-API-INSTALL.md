# Stage 2E — XGBoost API transport fix

This patch is applied to:
C:\Users\adilm\Projects\cost_prediction_xgboost_stage1

NO RETRAINING IS REQUIRED.

The API keeps XGBoost predictions for accommodation, food and activities. Transport uses the completed itinerary's route-based fare calculation so every travelled kilometre is charged and long journeys cannot be under-estimated by the transport model.
